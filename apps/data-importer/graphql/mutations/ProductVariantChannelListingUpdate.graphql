mutation ProductVariantChannelListingUpdate($productVariantId: ID!, $input: [ProductVariantChannelListingAddInput!]!) {
  productVariantChannelListingUpdate(input: $input, id: $productVariantId) {
    variant {
      id
    }
    errors {
      code
      message
    }
  }
}
